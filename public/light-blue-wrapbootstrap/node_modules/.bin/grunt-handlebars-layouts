#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-handlebars-layouts/bin/grunt-handlebars-layouts" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-handlebars-layouts/bin/grunt-handlebars-layouts" "$@"
  ret=$?
fi
exit $ret
